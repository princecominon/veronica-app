{
  "name": "veronica",
  "private": true,
  "version": "1.0.0",
  "description": "Veronica — AI Decision Platform for SAIL Shipping (Kotlin/JS + React port). CSS tooling only; the app itself is built with Gradle/Kotlin.",
  "scripts": {
    "css": "tailwindcss -c ./tailwind.config.cjs -i ./css/input.css -o ./web/styles.css --minify",
    "build": "gradle -p . jsBrowserDistribution && npm run css && bash ./scripts/assemble-dist.sh",
    "preview": "npx serve dist"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.6"
  }
}
